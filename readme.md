# XAAS (XSLT as a service)
This project provides a way to perform XSLT-transformations as a service over the AMQP-protocol.

## Usage
The easiest way to use the service is to pull the docker image [lenkan/xaas]()
